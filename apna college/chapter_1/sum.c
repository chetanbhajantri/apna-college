#include <stdio.h>
// the sum of two numbers
int main()
{
    int a, b;               
    printf("\n Enter a:");
    scanf("%d", &a);

    printf("\n Enter b:");
    scanf("%d", &b);

    printf("\n the sum is : %d", a + b);
    return 0;
}